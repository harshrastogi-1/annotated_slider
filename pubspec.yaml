name: annotated_slider
description: A Flutter package for creating sliders with customizable markers, tick marks, and annotations.
version: 0.0.1
homepage: https://github.com/harshrastogi-1/annotated_slider
repository: https://github.com/harshrastogi-1/annotated_slider
issue_tracker: https://github.com/harshrastogi-1/annotated_slider/issues

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/
